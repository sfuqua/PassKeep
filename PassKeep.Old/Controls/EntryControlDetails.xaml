<UserControl
    x:Name="root"
    x:Class="PassKeep.Controls.EntryControlDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PassKeep.Controls"
    xmlns:common="using:PassKeep.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400">

    <StackPanel>
        <TextBlock Style="{StaticResource EntryFieldLabel}">Notes</TextBlock>
        <TextBox ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollMode="Enabled"
                    Style="{StaticResource ReadOnlyFieldBox}" Height="400"
                    Text="{Binding Notes}" TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
